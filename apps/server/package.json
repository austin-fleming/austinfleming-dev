{
  "name": "@repo/server",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --platform=node --outfile=dist/server.js --external:express",
    "build:production": "yarn build --minify",
    "build:development": "yarn build --sourcemap",
    "dev": "nodemon",
    "audit": "audit",
    "test": "echo \"No tests written yet!\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "esbuild": "^0.14.34",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "express": "^4.17.3"
  }
}
